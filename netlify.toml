[build]
  base    = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/api/*"
  to     = "/api/:splat"
  status = 404

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
